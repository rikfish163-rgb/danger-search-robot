#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
WORKSPACE_DIR="$(cd "$SCRIPT_DIR/../../.." && pwd)"
exec "$WORKSPACE_DIR/.venv_vision/bin/python3" \
  "$SCRIPT_DIR/yolo_detector.py" \
  "$@"
