# Unitree A1 三层危险源自主搜索任务完整操作说明

**适用环境：** Ubuntu 20.04、ROS Noetic、Gazebo Classic、Unitree A1、Livox Mid-360、FAST-LIO、TEB、Graph NBV、危险源视觉识别  
**工作空间：** `~/catkin_ws`  
**项目包：** `~/catkin_ws/src/danger_search_robot`  
**版本：** 2026-07-31 限时探索与导航辅助修订版

**本次修订：**

```text
1. Graph NBV 固定使用 global_frame: world。
2. 每层 Graph NBV 使用 400 秒 ROS 仿真时间限制，超时自动取消导航目标并结束探索节点。
3. TEB 全局/局部代价地图 inflation_radius 由 0.12 m 小幅减至 0.10 m。
4. 候选视点净空距离仅小幅增加：min_candidate_clearance 增加 0.03 m，
   preferred_clearance 增加 0.05 m；其他 NBV 前沿与评分参数不改。
5. 修正 send_world_goal.py 的安装方式，不再从未知当前目录执行 cp。
```

---

## 1. 文档目的与任务总流程

本文档用于指导使用者完成一次完整的三层危险源自主搜索任务。整套任务由以下阶段组成：

```text
出生点启动系统
→ 打开楼栋大门和三层全部电梯厅门
→ 一楼 FAST-LIO、二维地图、导航和视觉初始化
→ 自动进入楼栋并到达一楼探索起点
→ 一楼自主探索和危险源识别
→ 返回电梯前并进入电梯
→ 保存一楼地图并切换到二楼
→ 二楼重新建立 SLAM 坐标链并加载二楼地图
→ 出电梯并到达二楼探索起点
→ 二楼自主探索和危险源识别
→ 返回电梯并切换到三楼
→ 三楼重新建立 SLAM 坐标链并加载三楼地图
→ 出电梯并到达三楼探索起点
→ 三楼自主探索和危险源识别
→ 返回电梯并直接下降到一楼
→ 加载一楼旧地图
→ 出电梯、经过楼栋入口并返回出生点
```

本文档合并了原有的“单层探索启动顺序”和“多层 SLAM 使用指南”，并纳入了最新验证结果。当前多层模式应统一使用 `world` 作为 RViz Fixed Frame；旧单层文档中使用 `map_level` 的表述不再作为本任务的执行标准。旧文档中的“手动开到走廊起点”也已由当前验证成功的航点导航替代。

---

## 2. 系统结构与楼层编号

多层 SLAM 采用以下结构：

```text
每层独立启动 FAST-LIO
统一使用 world 任务坐标系
每层独立保存二维投影地图
返回旧楼层时自动加载该楼层地图
```

系统关键状态文件为：

```text
当前楼层状态：
~/catkin_ws/results/floor_state.json

本次跨层锚点：
~/catkin_ws/results/floor_transition_anchor.json

各楼层二维地图内部状态：
~/catkin_ws/results/floors/floor_N/projection_state.bin
```

楼层编号固定为：

| 程序楼层编号 | 物理楼层 |
|---|---|
| `floor_0` / `0` | 一楼 |
| `floor_1` / `1` | 二楼 |
| `floor_2` / `2` | 三楼 |

当前导航使用的主要坐标链为：

```text
world
└── map_level
    └── camera_init
        └── body
```

二维地图发布到：

```text
/map_raw
/map_confirmed
```

导航与探索使用 `/map_confirmed`。多层任务中 RViz 必须设置：

```text
Fixed Frame: world
```

---

## 3. 已验证航点

航点文件：

```text
~/catkin_ws/src/danger_search_robot/config/mission_waypoints.yaml
```

当前已验证的世界坐标如下：

| 航点 | x | y | yaw | 用途 |
|---|---:|---:|---:|---|
| `robot_start` | `0.000` | `-3.200` | `1.5708` | 出生点及最终返回点 |
| `floor_0/entrance_inside` | `0.111` | `0.940` | `1.553` | 一楼进入楼栋后的经过点 |
| 各层 `exploration_start` | `0.023` | `7.452` | `1.583` | 各层探索起点 |
| 各层 `elevator_wait` | `0.320` | `2.361` | `0.000` | 电梯厅等待点 |
| 各层 `elevator_inside` | `2.557` | `2.339` | `0.000` | 电梯内部停车点 |

**重要：**

```text
exploration_start 的 yaw 必须约为 1.583，而不是 0。
elevator_wait 和 elevator_inside 的 yaw 为 0。
entrance_inside 的 yaw 约为 1.553。
```

---

## 4. 必须遵守的规则

### 4.1 所有门必须在启动 FAST-LIO 前打开

每次完整启动任务前，必须先打开：

```text
main_entrance
elevator_floor_0
elevator_floor_1
elevator_floor_2
```

不仅楼栋大门要打开，三层电梯厅门也全部要打开。电梯门在开启状态下仍可操纵电梯，因此任务中建议保持全部门开启。

如果关门状态已经被 FAST-LIO 或二维投影扫描进地图，调用：

```bash
rosservice call /move_base/clear_costmaps "{}"
```

无法消除 `/map_confirmed` 中已经形成的障碍。正确处理方法是停止相关 SLAM 节点、打开门，并按新任务流程重新建立地图。

### 4.2 整次新任务第一次启动二维投影后必须清空一次

仅在一次全新任务的第一次一楼二维投影启动后执行：

```bash
rosservice call /fastlio_2d_projection/clear_map "{}"
rosservice call /fastlio_2d_projection/clear_candidates "{}"
```

执行时机器人必须还在出生点静止。二楼和三楼第一次进入时，节点会自动创建该楼层的新空地图，不要再次调用 `clear_map`。任务进行中以及返回旧楼层时严禁清空地图。

### 4.3 跨层前必须先保存地图和执行 prepare

跨层固定顺序为：

```text
机器人进入电梯并停稳
→ save_current_floor
→ prepare --target-floor N
→ 确认 PREPARED
→ 停止探索、视觉、导航、二维投影、world 对齐、水平 TF 和 FAST-LIO
→ elevator_floor_transition.py move --open-doors
```

`prepare` 必须在旧楼层的 `world → body` 坐标链仍然存在时执行。

### 4.4 电梯移动期间 FAST-LIO 和二维投影必须停止

电梯移动时保持运行的只有：

```text
SimEnv
junior_ctrl
Livox bridge
```

不能让 FAST-LIO 或二维投影继续运行，否则电梯移动过程或目标楼层点云会被写入原楼层地图。

### 4.5 跨层后必须使用 reanchor

只有整次仿真首次启动的一楼使用：

```text
world_map_level_calibration.launch
```

所有跨层到达后的楼层，包括最后返回一楼，都必须使用：

```text
world_map_level_reanchor.launch
```

### 4.6 不要直接调用原始电梯服务完成正式跨层

正式多层流程必须使用：

```bash
rosrun danger_search_robot \
elevator_floor_transition.py \
move \
--open-doors
```

不要用 `/call_elevator` 代替，因为原始服务不会同步更新楼层状态文件和跨层锚点。

### 4.7 其他固定要求

- 所有 ROS 终端先退出 Conda `(base)`。
- 多层模式中 RViz Fixed Frame 使用 `world`。
- 不使用 `slam_stack.launch`。
- 不使用 `navigation_stack.launch`。
- 视觉只使用 `vision_stack.launch`。
- 当前不要对整个工作空间执行 `catkin_make`，避免再次触发 `livox_sdk.h` 缺失问题。
- `junior_ctrl` 首先按 `2` 站立；系统启动完成后再按 `6` 进入 `/cmd_vel` 模式。

---

## 5. 所有新终端的通用初始化

除运行 `auto.sh` 的终端外，每个新 ROS 终端先执行：

```bash
conda deactivate 2>/dev/null || true

source /opt/ros/noetic/setup.bash
source ~/catkin_ws/devel/setup.bash
```

---

## 6. 一次性准备导航目标发送脚本

本文后续使用 `send_world_goal.py` 发送单个 `world` 航点并等待 `move_base` 返回结果。该脚本必须真实存在于：

```text
~/catkin_ws/src/danger_search_robot/scripts/send_world_goal.py
```

不要再使用下面这种依赖当前目录中已有同名文件的命令：

```text
cp send_world_goal.py ...
```

在任意终端执行下面的自包含命令创建脚本：

```bash
mkdir -p \
~/catkin_ws/src/danger_search_robot/scripts

cat > \
~/catkin_ws/src/danger_search_robot/scripts/send_world_goal.py <<'PY'
#!/usr/bin/env python3
import math
import sys

import actionlib
import rospy
from actionlib_msgs.msg import GoalStatus
from move_base_msgs.msg import MoveBaseAction, MoveBaseGoal
from tf.transformations import quaternion_from_euler


def main():
    if len(sys.argv) != 5:
        print("用法: send_world_goal.py x y yaw label")
        return 2

    x = float(sys.argv[1])
    y = float(sys.argv[2])
    yaw = float(sys.argv[3])
    label = sys.argv[4]

    rospy.init_node("send_world_goal", anonymous=True)
    client = actionlib.SimpleActionClient("/move_base", MoveBaseAction)

    print("等待 /move_base action server...")
    if not client.wait_for_server(rospy.Duration(10.0)):
        print("错误：无法连接 /move_base")
        return 1

    q = quaternion_from_euler(0.0, 0.0, yaw)

    goal = MoveBaseGoal()
    goal.target_pose.header.frame_id = "world"
    goal.target_pose.header.stamp = rospy.Time.now()
    goal.target_pose.pose.position.x = x
    goal.target_pose.pose.position.y = y
    goal.target_pose.pose.position.z = 0.0
    goal.target_pose.pose.orientation.x = q[0]
    goal.target_pose.pose.orientation.y = q[1]
    goal.target_pose.pose.orientation.z = q[2]
    goal.target_pose.pose.orientation.w = q[3]

    print(f"发送目标：{label}")
    print("  frame = world")
    print(f"  x     = {x:.3f}")
    print(f"  y     = {y:.3f}")
    print(f"  yaw   = {yaw:.4f} rad = {math.degrees(yaw):.2f}°")

    client.send_goal(goal)
    client.wait_for_result()

    state = client.get_state()
    names = {
        GoalStatus.PENDING: "PENDING",
        GoalStatus.ACTIVE: "ACTIVE",
        GoalStatus.PREEMPTED: "PREEMPTED",
        GoalStatus.SUCCEEDED: "SUCCEEDED",
        GoalStatus.ABORTED: "ABORTED",
        GoalStatus.REJECTED: "REJECTED",
        GoalStatus.PREEMPTING: "PREEMPTING",
        GoalStatus.RECALLING: "RECALLING",
        GoalStatus.RECALLED: "RECALLED",
        GoalStatus.LOST: "LOST",
    }

    print(f"导航结果：{names.get(state, str(state))}")
    return 0 if state == GoalStatus.SUCCEEDED else 1


if __name__ == "__main__":
    raise SystemExit(main())
PY

chmod +x \
~/catkin_ws/src/danger_search_robot/scripts/send_world_goal.py
```

检查文件和执行权限：

```bash
ls -l \
~/catkin_ws/src/danger_search_robot/scripts/send_world_goal.py

head -n 1 \
~/catkin_ws/src/danger_search_robot/scripts/send_world_goal.py
```

第一行必须是：

```text
#!/usr/bin/env python3
```

刷新 ROS 环境后检查 `rosrun` 是否能找到：

```bash
conda deactivate 2>/dev/null || true
source /opt/ros/noetic/setup.bash
source ~/catkin_ws/devel/setup.bash

rospack find danger_search_robot

rosrun danger_search_robot \
send_world_goal.py
```

最后一条命令因为没有提供参数，应输出用法提示：

```text
用法: send_world_goal.py x y yaw label
```

这说明 `rosrun` 已经能找到脚本。该操作不需要 `catkin_make`。

正式使用格式：

```bash
rosrun danger_search_robot \
send_world_goal.py \
X Y YAW LABEL
```

成功时应输出：

```text
导航结果：SUCCEEDED
```

### 6.1 检查400秒仿真时间探索启动文件

新的探索启动文件为：

```text
~/catkin_ws/src/danger_search_robot/launch/graph_nbv_stage_b31_manual_gate_timed.launch
```

独立仿真时间看门狗为：

```text
~/catkin_ws/src/danger_search_robot/scripts/nbv_sim_time_watchdog.py
```

开始任务前检查：

```bash
ls -l \
~/catkin_ws/src/danger_search_robot/launch/graph_nbv_stage_b31_manual_gate_timed.launch \
~/catkin_ws/src/danger_search_robot/scripts/nbv_sim_time_watchdog.py

python3 -m py_compile \
~/catkin_ws/src/danger_search_robot/scripts/nbv_sim_time_watchdog.py
```

如果两个文件缺失，按照附录 C 创建。修改 Python、YAML 和 launch 文件后均不需要重新编译工作空间，只需要重启对应节点；代价地图参数需要重启 `move_base` 才能生效。

---

# 第一部分：新任务初始化与一楼启动

## 7. 终端 1：启动 SimEnv

```bash
conda deactivate 2>/dev/null || true

source /opt/ros/noetic/setup.bash
source ~/catkin_ws/devel/setup.bash

cd ~/catkin_ws/src/SimEnv
./auto.sh
```

等待 Gazebo、A1、控制器、门和电梯服务以及 `/clock` 正常启动。

在 `junior_ctrl` 终端按：

```text
2
```

让机器人站立。此时暂时不要按 `6`。

---

## 8. 启动 SLAM 前打开全部门

在新终端执行：

```bash
conda deactivate 2>/dev/null || true
source /opt/ros/noetic/setup.bash
source ~/catkin_ws/devel/setup.bash

for door_id in \
main_entrance \
elevator_floor_0 \
elevator_floor_1 \
elevator_floor_2
do
    echo "Opening ${door_id} ..."
    rosservice call /set_door_state \
    "{door_id: '${door_id}', open: true}"
done
```

在 Gazebo 中确认四扇门都已经完全打开后再继续。

**此步骤必须在首次 FAST-LIO 启动前完成。**

---

## 9. 清空旧任务地图并初始化楼层状态

该步骤只在开始一次全新任务时执行。机器人、电梯均应实际位于一楼。

```bash
rm -rf ~/catkin_ws/results/floors
mkdir -p ~/catkin_ws/results/floors

rm -f \
~/catkin_ws/results/floor_transition_anchor.json
```

初始化楼层状态：

```bash
rosrun danger_search_robot \
elevator_floor_transition.py \
init \
--current-floor 0 \
--floor-height 2.6 \
--floor-count 3 \
--force
```

检查：

```bash
rosrun danger_search_robot \
elevator_floor_transition.py \
show
```

预期：

```text
current_floor = 0
previous_floor = null
last_transition_id = null
```

如果 SimEnv 没有重启，只是重启某个 SLAM 节点，不要执行本节，不要再次 `init --force`。

---

## 10. 终端 2：启动 Livox bridge

```bash
conda deactivate 2>/dev/null || true

source /opt/ros/noetic/setup.bash
source ~/catkin_ws/devel/setup.bash

python3 \
~/catkin_ws/src/danger_search_robot/sensor_adapter/scripts/unitree_livox_bridge.py
```

检查话题：

```bash
rostopic list | grep livox
```

检查实际 FAST-LIO 配置所使用的点云话题是否持续发布。若当前配置使用 `/livox/lidar`：

```bash
rostopic hz /livox/lidar
```

如果 `mapping_mid360.launch` 或 YAML 中配置的是其他 `lid_topic`，以实际配置为准。

---

## 11. 终端 3：启动一楼 FAST-LIO

再次确认全部门已经打开，然后执行：

```bash
conda deactivate 2>/dev/null || true

source /opt/ros/noetic/setup.bash
source ~/catkin_ws/devel/setup.bash

roslaunch fast_lio mapping_mid360.launch
```

检查：

```bash
rostopic hz /Odometry
```

```bash
rostopic hz /cloud_registered_body
```

两者持续有数据后继续。

---

## 12. 终端 4：启动水平坐标转换

```bash
conda deactivate 2>/dev/null || true

source /opt/ros/noetic/setup.bash
source ~/catkin_ws/devel/setup.bash

roslaunch danger_search_robot \
fastlio_level_tf.launch
```

检查：

```bash
rosrun tf tf_echo map_level body
```

应连续输出坐标。

---

## 13. 终端 5：一楼 world 初始标定

机器人必须在出生点保持静止：

```bash
conda deactivate 2>/dev/null || true

source /opt/ros/noetic/setup.bash
source ~/catkin_ws/devel/setup.bash

roslaunch danger_search_robot \
world_map_level_calibration.launch
```

检查完整坐标链：

```bash
timeout 8s rosrun tf tf_echo world body
```

必须连续输出：

```text
At time ...
- Translation: [...]
- Rotation: ...
```

首次一楼建立的是：

```text
world → map_level → camera_init → body
```

---

## 14. 终端 6：启动一楼二维投影并清空初始地图

必须确认 `world → body` 已经正常后再启动：

```bash
conda deactivate 2>/dev/null || true

source /opt/ros/noetic/setup.bash
source ~/catkin_ws/devel/setup.bash

roslaunch \
~/catkin_ws/src/danger_search_robot/mapping/launch/fastlio_2d_projection.launch
```

第一次进入一楼应显示：

```text
Starting floor 0 with a new empty 2D map
```

如果显示：

```text
Loaded floor 0 map
```

说明旧地图状态仍存在，应检查第 9 节是否正确清理。

在本次任务第一次启动二维投影后，机器人保持静止，执行一次：

```bash
rosservice call \
/fastlio_2d_projection/clear_map "{}"

rosservice call \
/fastlio_2d_projection/clear_candidates "{}"
```

检查当前楼层：

```bash
rostopic echo -n 1 \
/fastlio_2d_projection/current_floor
```

预期：

```text
data: 0
```

检查地图：

```bash
rostopic echo -n 1 /map_confirmed/header
rostopic hz /map_confirmed
```

当前多层任务标准中，RViz Fixed Frame 使用 `world`。

---

## 15. 终端 7：启动 move_base 和 TEB

```bash
conda deactivate 2>/dev/null || true

source /opt/ros/noetic/setup.bash
source ~/catkin_ws/devel/setup.bash

roslaunch danger_search_robot \
move_base_teb.launch
```

检查：

```bash
rosnode ping -c 2 /move_base
```

```bash
rosparam get /move_base/base_local_planner
```

应返回：

```text
teb_local_planner/TebLocalPlannerROS
```

```bash
rosparam get /move_base/base_global_planner
```

应返回：

```text
global_planner/GlobalPlanner
```

检查规划服务：

```bash
rosservice list | grep '^/move_base/make_plan$'
```

检查当前关键参数：

```bash
rosparam get /move_base/global_costmap/global_frame
rosparam get /move_base/global_costmap/robot_base_frame
rosparam get /move_base/local_costmap/robot_base_frame
rosparam get /move_base/TebLocalPlannerROS/yaw_goal_tolerance
```

当前预期：

```text
global costmap global_frame = world
global/local robot_base_frame = body
yaw_goal_tolerance = 0.08
```

膨胀参数应为：

```text
inflation_radius = 0.10
cost_scaling_factor = 12.0
robot_radius = 0.20
```

检查 Graph NBV 的世界坐标系和候选视点净空参数：

```bash
grep -nE \
"global_frame|min_candidate_clearance|preferred_clearance|visibility_wall_dilation" \
~/catkin_ws/src/danger_search_robot/exploration/graph_nbv/config/graph_nbv_stage_b31_manual_gate.yaml
```

要求：

```text
global_frame 必须是 world
min_candidate_clearance 为恢复版原值 + 0.03 m
preferred_clearance 为恢复版原值 + 0.05 m
其他前沿阈值、评分权重和 visibility_wall_dilation 暂不修改
```

---

## 16. 终端 8：启动危险源视觉栈

```bash
conda deactivate 2>/dev/null || true

source /opt/ros/noetic/setup.bash
source ~/catkin_ws/devel/setup.bash

roslaunch danger_search_robot \
vision_stack.launch
```

视觉统一入口包含 YOLO、RGB-D 定位、TargetManager 和 ResultWriter，不要分别启动。

等待约 8 秒后检查：

```bash
rosnode list | grep -Ei \
"yolo|vision|localization|target|writer"
```

监视危险源结果：

```bash
watch -n 1 \
'cat ~/catkin_ws/results/detected_danger.json 2>/dev/null || echo waiting'
```

视觉启动时可能出现不影响结果的提示或报错，判断标准应以关键节点是否存在、图像和检测输出是否正常为准。

---

## 17. 切换机器人到 `/cmd_vel` 模式

所有系统启动完成后，回到 `auto.sh` / `junior_ctrl` 终端按：

```text
6
```

机器人进入 RL `/cmd_vel` 控制模式。

首次启动的严格顺序为：

```text
auto.sh
→ 按 2
→ 打开全部门
→ 初始化楼层状态
→ Livox bridge
→ FAST-LIO
→ fastlio_level_tf
→ world_map_level_calibration
→ 检查 world → body
→ fastlio_2d_projection
→ 首次 clear_map 和 clear_candidates
→ move_base_teb（重新加载 inflation_radius = 0.10）
→ vision_stack
→ 按 6
→ mission_manager 到一楼探索起点
→ Graph NBV timed（400 秒 ROS 仿真时间）
```

---

# 第二部分：一楼任务

## 18. 自动进入楼栋并到达一楼探索起点

执行已经验证的任务节点：

```bash
conda deactivate 2>/dev/null || true
source /opt/ros/noetic/setup.bash
source ~/catkin_ws/devel/setup.bash

rosrun danger_search_robot mission_manager.py \
  _floor:=0 \
  _goal_frame:=world \
  _robot_frame:=body \
  _intermediate_reach_tolerance:=0.35 \
  _goal_timeout:=0
```

该任务按以下顺序运行：

```text
出生区域
→ entrance_inside
→ exploration_start
```

其中 `entrance_inside` 是经过点，只按距离判定；最终 `exploration_start` 等待 `move_base` 返回 `SUCCEEDED`，并执行最终朝向。

确认机器人位于：

```text
x ≈ 0.023
y ≈ 7.452
yaw ≈ 1.583 rad
```

---

## 19. 启动一楼 Graph NBV 探索

机器人在探索起点停稳后启动：

```bash
conda deactivate 2>/dev/null || true

source /opt/ros/noetic/setup.bash
source ~/catkin_ws/devel/setup.bash

roslaunch danger_search_robot \
graph_nbv_stage_b31_manual_gate_timed.launch \
exploration_dry_run:=false \
exploration_time_limit:=400.0
```

该 400 秒使用 `/clock` 的 ROS 仿真时间。Gazebo 暂停时不计时；实时因子低于 1 时，现实等待时间会更长。到达限制后，看门狗会：

```text
取消 /move_base 当前目标
→ 发布零速度
→ 关闭 /graph_nbv
→ 结束本层探索
```

检查关键参数：

```bash
rosparam get /use_sim_time
rosparam get /graph_nbv/global_frame
rosparam get /graph_nbv/min_candidate_clearance
rosparam get /graph_nbv/preferred_clearance

rosnode list | grep -E \
"graph_nbv|nbv_sim_time_watchdog"
```

必须确认：

```text
/use_sim_time = true
/graph_nbv/global_frame = world
```

检查状态：

```bash
rostopic echo /graph_nbv/status
```

检查门控：

```bash
rostopic echo /graph_nbv/gate_state
```

初始通常应看到：

```text
FORWARD_REGION_LOCKED
```

探索过程中同时观察：

```bash
watch -n 1 \
'cat ~/catkin_ws/results/detected_danger.json 2>/dev/null || echo waiting'
```

一楼探索可能因算法自行完成、人工 `Ctrl+C` 或达到 400 秒仿真时间而结束。确认 `/graph_nbv` 已停止后，保留 `move_base`、视觉、二维投影和 FAST-LIO，用于返回电梯。若 timed launch 终端仍未退出，再按一次 `Ctrl+C` 关闭该启动终端。

---

## 20. 一楼返回电梯并进入电梯

先导航到电梯前：

```bash
rosrun danger_search_robot \
send_world_goal.py \
0.320 2.361 0.0 floor_0_elevator_wait
```

必须等待：

```text
导航结果：SUCCEEDED
```

全部电梯门在任务开始前已经打开，因此不调用 `clear_costmaps`，也不需要重新开门。然后进入电梯：

```bash
rosrun danger_search_robot \
send_world_goal.py \
2.557 2.339 0.0 floor_0_elevator_inside
```

等待：

```text
导航结果：SUCCEEDED
```

机器人进入电梯后保持静止：

```bash
rostopic pub -1 /cmd_vel geometry_msgs/Twist \
"linear:
  x: 0.0
  y: 0.0
  z: 0.0
angular:
  x: 0.0
  y: 0.0
  z: 0.0"
```

```bash
sleep 3
```

---

# 第三部分：一楼切换到二楼

## 21. 保存一楼地图

```bash
rosservice call \
/fastlio_2d_projection/save_current_floor "{}"
```

检查：

```bash
ls -lh \
~/catkin_ws/results/floors/floor_0/projection_state.bin
```

---

## 22. 在一楼坐标链仍存在时执行 prepare

```bash
rosrun danger_search_robot \
elevator_floor_transition.py \
prepare \
--target-floor 1 \
--sample-count 20 \
--sample-rate 10 \
--max-translation-std 0.08
```

检查：

```bash
rosrun danger_search_robot \
elevator_floor_transition.py \
show
```

预期包含：

```text
current_floor = 0
status = PREPARED
source_floor = 0
target_floor = 1
delta_z = 2.6
```

---

## 23. 停止一楼相关节点

`prepare` 成功后，依次在对应终端按 `Ctrl+C` 停止：

```text
1. Graph NBV timed / 看门狗（此时通常已经停止）
2. vision_stack
3. move_base_teb
4. fastlio_2d_projection
5. world_map_level_calibration
6. fastlio_level_tf
7. FAST-LIO
```

继续保持：

```text
SimEnv
junior_ctrl
Livox bridge
```

---

## 24. 移动电梯到二楼

```bash
rosrun danger_search_robot \
elevator_floor_transition.py \
move \
--open-doors
```

到达后检查：

```bash
rosrun danger_search_robot \
elevator_floor_transition.py \
show
```

预期：

```text
previous_floor = 0
current_floor = 1
status = ELEVATOR_ARRIVED
source_floor = 0
target_floor = 1
```

机器人保持静止，二楼电梯门保持开启。

---

# 第四部分：二楼任务

## 25. 二楼重启 SLAM 坐标链

### 终端 A：FAST-LIO

```bash
roslaunch fast_lio mapping_mid360.launch
```

### 终端 B：水平 TF

```bash
roslaunch danger_search_robot \
fastlio_level_tf.launch
```

### 终端 C：world 重锚定

```bash
roslaunch danger_search_robot \
world_map_level_reanchor.launch
```

检查：

```bash
rostopic echo -n 1 /world_alignment/status
```

二楼应为：

```text
REANCHORED_FLOOR_1
```

检查楼层：

```bash
rostopic echo -n 1 \
/world_alignment/current_floor
```

检查坐标链：

```bash
timeout 8s rosrun tf tf_echo world body
```

只有 `world → body` 连续正常后，才能启动二楼二维投影。

---

## 26. 启动二楼二维投影

```bash
roslaunch \
~/catkin_ws/src/danger_search_robot/mapping/launch/fastlio_2d_projection.launch
```

第一次进入二楼应显示：

```text
Starting floor 1 with a new empty 2D map
```

如果以前已经探索过并保存，则显示：

```text
Loaded floor 1 map
```

检查：

```bash
rostopic echo -n 1 \
/fastlio_2d_projection/current_floor
```

预期：

```text
data: 1
```

**二楼不要调用 `clear_map`。**

---

## 27. 重启二楼导航和视觉

导航：

```bash
roslaunch danger_search_robot \
move_base_teb.launch
```

视觉：

```bash
roslaunch danger_search_robot \
vision_stack.launch
```

`junior_ctrl` 在整个跨层过程中保持运行，通常仍处于 `6` 模式；确认机器人能够接收 `/cmd_vel` 即可。

---

## 28. 二楼出电梯并到达探索起点

先离开电梯到等待点：

```bash
rosrun danger_search_robot \
send_world_goal.py \
0.320 2.361 0.0 floor_1_elevator_wait
```

再到二楼探索起点：

```bash
rosrun danger_search_robot \
send_world_goal.py \
0.023 7.452 1.583 floor_1_exploration_start
```

两个目标都必须返回：

```text
导航结果：SUCCEEDED
```

---

## 29. 二楼探索和危险源识别

```bash
roslaunch danger_search_robot \
graph_nbv_stage_b31_manual_gate_timed.launch \
exploration_dry_run:=false \
exploration_time_limit:=400.0
```

使用与一楼相同的状态、门控、400 秒仿真时间看门狗和视觉结果检查命令。算法自行完成、人工停止或超时后，确认 `/graph_nbv` 已停止；保留导航、视觉、二维投影和 FAST-LIO，返回电梯。

---

## 30. 二楼返回电梯

```bash
rosrun danger_search_robot \
send_world_goal.py \
0.320 2.361 0.0 floor_1_elevator_wait_return
```

```bash
rosrun danger_search_robot \
send_world_goal.py \
2.557 2.339 3.1416 floor_1_elevator_inside
```

机器人停稳后发送零速度并等待 3 秒。

---

# 第五部分：二楼切换到三楼

## 31. 保存二楼地图

```bash
rosservice call \
/fastlio_2d_projection/save_current_floor "{}"
```

检查：

```bash
ls -lh \
~/catkin_ws/results/floors/floor_1/projection_state.bin
```

---

## 32. 执行二楼到三楼 prepare

```bash
rosrun danger_search_robot \
elevator_floor_transition.py \
prepare \
--target-floor 2 \
--sample-count 20 \
--sample-rate 10 \
--max-translation-std 0.08
```

检查 `show`，预期：

```text
current_floor = 1
status = PREPARED
source_floor = 1
target_floor = 2
delta_z = 2.6
```

---

## 33. 停止二楼节点并移动电梯

依次停止：

```text
Graph NBV timed / 看门狗
vision_stack
move_base_teb
fastlio_2d_projection
world_map_level_reanchor
fastlio_level_tf
FAST-LIO
```

保持 SimEnv、junior_ctrl、Livox bridge。

移动：

```bash
rosrun danger_search_robot \
elevator_floor_transition.py \
move \
--open-doors
```

检查：

```bash
rosrun danger_search_robot \
elevator_floor_transition.py \
show
```

预期当前楼层为：

```text
current_floor = 2
```

---

# 第六部分：三楼任务

## 34. 三楼重启 SLAM、二维地图、导航和视觉

依次启动：

```bash
roslaunch fast_lio mapping_mid360.launch
```

```bash
roslaunch danger_search_robot \
fastlio_level_tf.launch
```

```bash
roslaunch danger_search_robot \
world_map_level_reanchor.launch
```

检查：

```bash
rostopic echo -n 1 /world_alignment/status
```

预期：

```text
REANCHORED_FLOOR_2
```

检查 `world → body` 后启动：

```bash
roslaunch \
~/catkin_ws/src/danger_search_robot/mapping/launch/fastlio_2d_projection.launch
```

第一次进入三楼应显示：

```text
Starting floor 2 with a new empty 2D map
```

检查：

```bash
rostopic echo -n 1 \
/fastlio_2d_projection/current_floor
```

预期：

```text
data: 2
```

启动导航：

```bash
roslaunch danger_search_robot \
move_base_teb.launch
```

启动视觉：

```bash
roslaunch danger_search_robot \
vision_stack.launch
```

---

## 35. 三楼出电梯并到达探索起点

```bash
rosrun danger_search_robot \
send_world_goal.py \
0.320 2.361 0.0 floor_2_elevator_wait
```

```bash
rosrun danger_search_robot \
send_world_goal.py \
0.023 7.452 1.583 floor_2_exploration_start
```

---

## 36. 三楼探索和危险源识别

```bash
roslaunch danger_search_robot \
graph_nbv_stage_b31_manual_gate_timed.launch \
exploration_dry_run:=false \
exploration_time_limit:=400.0
```

三楼探索自行完成、人工停止或达到 400 秒仿真时间后，确认 `/graph_nbv` 已停止，再导航回电梯：

```bash
rosrun danger_search_robot \
send_world_goal.py \
0.320 2.361 0.0 floor_2_elevator_wait_return
```

```bash
rosrun danger_search_robot \
send_world_goal.py \
2.557 2.339 0.0 floor_2_elevator_inside
```

机器人停稳，发送零速度并等待 3 秒。

---

# 第七部分：三楼直接返回一楼

## 37. 保存三楼地图

```bash
rosservice call \
/fastlio_2d_projection/save_current_floor "{}"
```

检查：

```bash
ls -lh \
~/catkin_ws/results/floors/floor_2/projection_state.bin
```

---

## 38. 执行三楼到一楼 prepare

```bash
rosrun danger_search_robot \
elevator_floor_transition.py \
prepare \
--target-floor 0 \
--sample-count 20 \
--sample-rate 10 \
--max-translation-std 0.08
```

检查：

```bash
rosrun danger_search_robot \
elevator_floor_transition.py \
show
```

预期：

```text
current_floor = 2
status = PREPARED
source_floor = 2
target_floor = 0
delta_z = -5.2
```

---

## 39. 停止三楼节点并下降到一楼

依次停止：

```text
Graph NBV timed / 看门狗
vision_stack
move_base_teb
fastlio_2d_projection
world_map_level_reanchor
fastlio_level_tf
FAST-LIO
```

移动电梯：

```bash
rosrun danger_search_robot \
elevator_floor_transition.py \
move \
--open-doors
```

检查：

```bash
rosrun danger_search_robot \
elevator_floor_transition.py \
show
```

预期：

```text
previous_floor = 2
current_floor = 0
status = ELEVATOR_ARRIVED
source_floor = 2
target_floor = 0
```

---

# 第八部分：返回一楼并导航到出生点

## 40. 在一楼重新加载旧地图

依次启动：

```bash
roslaunch fast_lio mapping_mid360.launch
```

```bash
roslaunch danger_search_robot \
fastlio_level_tf.launch
```

```bash
roslaunch danger_search_robot \
world_map_level_reanchor.launch
```

检查：

```bash
rostopic echo -n 1 /world_alignment/status
```

预期：

```text
REANCHORED_FLOOR_0
```

检查：

```bash
timeout 8s rosrun tf tf_echo world body
```

启动一楼二维投影：

```bash
roslaunch \
~/catkin_ws/src/danger_search_robot/mapping/launch/fastlio_2d_projection.launch
```

必须显示：

```text
Loaded floor 0 map
```

此时严禁调用 `clear_map`。

检查当前楼层：

```bash
rostopic echo -n 1 \
/fastlio_2d_projection/current_floor
```

预期：

```text
data: 0
```

最终返回只需要导航，因此启动：

```bash
roslaunch danger_search_robot \
move_base_teb.launch
```

视觉任务已经结束时可以不再启动 `vision_stack`。

---

## 41. 从电梯返回出生点

先出电梯：

```bash
rosrun danger_search_robot \
send_world_goal.py \
0.320 2.361 0.0 floor_0_elevator_wait_final
```

然后经过楼栋入口内侧：

```bash
rosrun danger_search_robot \
send_world_goal.py \
0.111 0.940 1.553 floor_0_entrance_inside_return
```

最后返回出生点：

```bash
rosrun danger_search_robot \
send_world_goal.py \
0.000 -3.200 1.5708 robot_start_final
```

三个目标均应返回：

```text
导航结果：SUCCEEDED
```

机器人到达出生点后发送零速度：

```bash
rostopic pub -1 /cmd_vel geometry_msgs/Twist \
"linear:
  x: 0.0
  y: 0.0
  z: 0.0
angular:
  x: 0.0
  y: 0.0
  z: 0.0"
```

至此完成整次三层危险源自主搜索任务。

---

# 第九部分：RViz 推荐显示

RViz 设置：

```text
Fixed Frame: world
```

建议添加：

```text
TF
Map: /map_confirmed
Map: /move_base/global_costmap/costmap
Map: /move_base/local_costmap/costmap
Pose: /move_base/current_goal
Path: /move_base/GlobalPlanner/plan
Path: /move_base/TebLocalPlannerROS/local_plan
MarkerArray: /mission_waypoints
```

可选启动航点可视化：

```bash
rosrun danger_search_robot \
publish_mission_waypoints.py \
_floor:=0 \
_frame_id:=world
```

切换楼层时可把 `_floor` 改为 `1` 或 `2`。

---

# 第十部分：常用检查命令

## 42. 查看楼层管理状态

```bash
rosrun danger_search_robot \
elevator_floor_transition.py \
show
```

## 43. 查看二维投影当前楼层

```bash
rostopic echo -n 1 \
/fastlio_2d_projection/current_floor
```

## 44. 查看 world 对齐状态

```bash
rostopic echo -n 1 \
/world_alignment/status
```

## 45. 查看完整世界坐标链

```bash
timeout 8s rosrun tf tf_echo world body
```

## 46. 查看各层地图保存文件

```bash
find ~/catkin_ws/results/floors \
-type f \
-name projection_state.bin \
-ls
```

## 47. 强制保存当前楼层地图

```bash
rosservice call \
/fastlio_2d_projection/save_current_floor "{}"
```

## 48. 查看导航状态

```bash
rostopic echo /move_base/status
```

## 49. 查看探索状态

```bash
rostopic echo /graph_nbv/status
```

```bash
rostopic echo /graph_nbv/gate_state
```

## 50. 查看危险源结果

```bash
watch -n 1 \
'cat ~/catkin_ws/results/detected_danger.json 2>/dev/null || echo waiting'
```

---

# 第十一部分：故障判断

## 51. 机器人无法通过门洞

首先在 Gazebo 中确认门是否在启动 FAST-LIO 前已经打开。若门关闭状态已经写入 `/map_confirmed`，`clear_costmaps` 无法解决；必须按新任务或当前楼层重建流程处理。

同时检查膨胀参数：

```bash
rosparam get \
/move_base/global_costmap/inflation_layer/inflation_radius

rosparam get \
/move_base/local_costmap/inflation_layer/inflation_radius
```

预期均为：

```text
0.10
```

## 52. 目标位置正确但探索起点朝向错误

检查航点文件：

```bash
grep -nA3 "exploration_start:" \
~/catkin_ws/src/danger_search_robot/config/mission_waypoints.yaml
```

三层应全部为：

```text
yaw: 1.583
```

检查：

```bash
rosparam get \
/move_base/TebLocalPlannerROS/yaw_goal_tolerance
```

预期：

```text
0.08
```

不要再用 `yaw ± 90°` 进行猜测补偿。

## 53. 跨层后二维地图混入旧楼层或电梯点云

通常是电梯移动时 FAST-LIO 或二维投影没有停止。应停止当前导航和 SLAM，核对 `floor_state.json` 和 `elevator_floor_transition.py show`，然后按目标楼层重启顺序重新建立坐标链。不要在任务中随意删除全部楼层地图。

## 54. 二维投影启动失败或地图不更新

依次确认：

```text
1. FAST-LIO 正常输出 /Odometry 和点云。
2. fastlio_level_tf 正常。
3. world_map_level_calibration 或 reanchor 正常。
4. world → body 连续存在。
5. floor_state.json 当前楼层正确。
6. 然后才启动 fastlio_2d_projection。
```

## 55. 视觉启动有提示或错误

不要只看终端是否出现红字。检查关键节点和结果文件：

```bash
rosnode list | grep -Ei \
"yolo|vision|localization|target|writer"
```

```bash
cat ~/catkin_ws/results/detected_danger.json
```


## 55A. `rosrun` 找不到 `send_world_goal.py`

报错：

```text
[rosrun] Couldn't find executable named send_world_goal.py
```

首先检查文件是否存在并可执行：

```bash
ls -l \
~/catkin_ws/src/danger_search_robot/scripts/send_world_goal.py
```

如果显示“没有那个文件”，重新执行第 6 节的 `cat > ... <<'PY'` 创建命令。如果文件存在但没有执行权限：

```bash
chmod +x \
~/catkin_ws/src/danger_search_robot/scripts/send_world_goal.py
```

刷新环境：

```bash
conda deactivate 2>/dev/null || true
source /opt/ros/noetic/setup.bash
source ~/catkin_ws/devel/setup.bash

rospack find danger_search_robot
rosrun danger_search_robot send_world_goal.py
```

不需要执行 `catkin_make`。紧急情况下也可以绕过 `rosrun`，直接运行：

```bash
python3 \
~/catkin_ws/src/danger_search_robot/scripts/send_world_goal.py \
0.320 2.361 0.0 floor_0_elevator_wait
```

---

# 第十二部分：禁止操作清单

正常任务中禁止：

```text
1. 未打开全部门就启动或重启 FAST-LIO。
2. 在任务中依靠 clear_costmaps 删除已经写入二维地图的门。
3. 不经过 save_current_floor 就跨层。
4. 不经过 prepare 就停止旧楼层坐标链。
5. prepare 尚未成功就关闭 FAST-LIO。
6. 电梯移动时继续运行 FAST-LIO 或二维投影。
7. 直接调用 /call_elevator 代替 elevator_floor_transition.py move。
8. world → body 尚未建立就启动二维投影。
9. 跨层后继续使用 world_map_level_calibration。
10. 二楼、三楼或返回旧楼层时调用 clear_map。
11. 任务进行中删除 ~/catkin_ws/results/floors。
12. SimEnv 没有重启却再次执行 init --force。
13. 多层模式把 RViz Fixed Frame 设置为 map_level。
14. 让不同楼层共用一个未重启的二维投影节点。
15. 使用已废弃的 slam_stack.launch 或 navigation_stack.launch。
16. 对整个工作空间直接执行 catkin_make。
17. 随意修改已验证的航点 x、y 或把 exploration_start yaw 改成 0。
```

---

# 第十三部分：最简全流程速查

## 56. 首次一楼启动

```text
SimEnv → 按 2
→ 打开 main_entrance 和三层全部电梯门
→ 清空旧楼层地图并 init floor 0
→ Livox bridge
→ FAST-LIO
→ fastlio_level_tf
→ world_map_level_calibration
→ 检查 world → body
→ fastlio_2d_projection
→ 第一次 clear_map + clear_candidates
→ move_base_teb
→ vision_stack
→ 按 6
→ mission_manager 到一楼探索起点
→ Graph NBV timed（400 秒 ROS 仿真时间）
```

## 57. 当前楼层探索结束并离开

```text
确认 Graph NBV timed 已结束
→ elevator_wait
→ elevator_inside
→ 零速度并停稳
→ save_current_floor
→ prepare --target-floor N
→ 检查 PREPARED
→ 停止 vision / move_base / 2D projection / world 对齐 / level TF / FAST-LIO
→ move --open-doors
```

## 58. 到达新楼层

```text
FAST-LIO
→ fastlio_level_tf
→ world_map_level_reanchor
→ 检查 REANCHORED_FLOOR_N
→ 检查 world → body
→ fastlio_2d_projection
→ 检查 Starting floor N 或 Loaded floor N
→ move_base_teb
→ vision_stack
→ elevator_wait
→ exploration_start
→ Graph NBV timed（400 秒 ROS 仿真时间）
```

## 59. 三楼任务完成后

```text
返回三楼电梯
→ save floor_2
→ prepare --target-floor 0
→ 停止三楼节点
→ move --open-doors
→ 一楼 FAST-LIO
→ fastlio_level_tf
→ world_map_level_reanchor
→ Loaded floor 0 map
→ move_base
→ elevator_wait
→ entrance_inside
→ robot_start
→ 零速度
```

---

# 附录 A：关键文件

```text
仿真：
~/catkin_ws/src/SimEnv/auto.sh

Livox bridge：
~/catkin_ws/src/danger_search_robot/sensor_adapter/scripts/unitree_livox_bridge.py

FAST-LIO：
fast_lio/mapping_mid360.launch

水平坐标转换：
danger_search_robot/fastlio_level_tf.launch

首次一楼 world 标定：
danger_search_robot/world_map_level_calibration.launch

跨层 world 重锚定：
danger_search_robot/world_map_level_reanchor.launch

二维投影：
~/catkin_ws/src/danger_search_robot/mapping/launch/fastlio_2d_projection.launch

导航：
danger_search_robot/move_base_teb.launch

视觉：
danger_search_robot/vision_stack.launch

探索（400秒仿真时间）：
danger_search_robot/graph_nbv_stage_b31_manual_gate_timed.launch

探索主启动文件：
danger_search_robot/graph_nbv_stage_b31_manual_gate.launch

探索限时看门狗：
danger_search_robot/scripts/nbv_sim_time_watchdog.py

楼层切换：
danger_search_robot/scripts/elevator_floor_transition.py

一楼初始任务：
danger_search_robot/scripts/mission_manager.py

航点配置：
danger_search_robot/config/mission_waypoints.yaml

航点可视化：
danger_search_robot/scripts/publish_mission_waypoints.py

单点导航辅助：
danger_search_robot/scripts/send_world_goal.py
```

---

# 附录 B：`send_world_goal.py`

```python
#!/usr/bin/env python3
import math
import sys

import actionlib
import rospy
from actionlib_msgs.msg import GoalStatus
from move_base_msgs.msg import MoveBaseAction, MoveBaseGoal
from tf.transformations import quaternion_from_euler


def main():
    if len(sys.argv) != 5:
        print("用法: send_world_goal.py x y yaw label")
        return 2

    x = float(sys.argv[1])
    y = float(sys.argv[2])
    yaw = float(sys.argv[3])
    label = sys.argv[4]

    rospy.init_node("send_world_goal", anonymous=True)
    client = actionlib.SimpleActionClient("/move_base", MoveBaseAction)

    print("等待 /move_base action server...")
    if not client.wait_for_server(rospy.Duration(10.0)):
        print("错误：无法连接 /move_base")
        return 1

    q = quaternion_from_euler(0.0, 0.0, yaw)

    goal = MoveBaseGoal()
    goal.target_pose.header.frame_id = "world"
    goal.target_pose.header.stamp = rospy.Time.now()
    goal.target_pose.pose.position.x = x
    goal.target_pose.pose.position.y = y
    goal.target_pose.pose.position.z = 0.0
    goal.target_pose.pose.orientation.x = q[0]
    goal.target_pose.pose.orientation.y = q[1]
    goal.target_pose.pose.orientation.z = q[2]
    goal.target_pose.pose.orientation.w = q[3]

    print(f"发送目标：{label}")
    print("  frame = world")
    print(f"  x     = {x:.3f}")
    print(f"  y     = {y:.3f}")
    print(f"  yaw   = {yaw:.4f} rad = {math.degrees(yaw):.2f}°")

    client.send_goal(goal)
    client.wait_for_result()

    state = client.get_state()
    names = {
        GoalStatus.PENDING: "PENDING",
        GoalStatus.ACTIVE: "ACTIVE",
        GoalStatus.PREEMPTED: "PREEMPTED",
        GoalStatus.SUCCEEDED: "SUCCEEDED",
        GoalStatus.ABORTED: "ABORTED",
        GoalStatus.REJECTED: "REJECTED",
        GoalStatus.PREEMPTING: "PREEMPTING",
        GoalStatus.RECALLING: "RECALLING",
        GoalStatus.RECALLED: "RECALLED",
        GoalStatus.LOST: "LOST",
    }

    print(f"导航结果：{names.get(state, str(state))}")
    return 0 if state == GoalStatus.SUCCEEDED else 1


if __name__ == "__main__":
    raise SystemExit(main())
```

---

# 附录 C：400秒仿真时间探索启动文件

仅在以下文件不存在时执行一次。

创建看门狗：

```bash
mkdir -p \
~/catkin_ws/src/danger_search_robot/scripts

cat > \
~/catkin_ws/src/danger_search_robot/scripts/nbv_sim_time_watchdog.py <<'PY'
#!/usr/bin/env python3

import time

import rosnode
import rospy
from actionlib_msgs.msg import GoalID
from geometry_msgs.msg import Twist


def find_graph_nbv_nodes(exact_name):
    try:
        nodes = rosnode.get_node_names()
    except Exception as exc:
        rospy.logwarn_throttle(
            5.0,
            "[nbv_watchdog] 无法读取节点列表：%s",
            exc,
        )
        return []

    own_name = rospy.get_name()

    if exact_name in nodes:
        return [exact_name]

    return [
        node
        for node in nodes
        if (
            "graph_nbv" in node.lower()
            and "watchdog" not in node.lower()
            and node != own_name
        )
    ]


def publish_stop(cancel_pub, cmd_vel_pub):
    cancel_msg = GoalID()
    zero_cmd = Twist()

    for _ in range(8):
        cancel_pub.publish(cancel_msg)
        cmd_vel_pub.publish(zero_cmd)
        time.sleep(0.05)


def main():
    rospy.init_node("nbv_sim_time_watchdog")

    duration = float(rospy.get_param("~duration", 400.0))
    target_node = str(
        rospy.get_param("~target_node", "/graph_nbv")
    )
    cancel_topic = str(
        rospy.get_param("~cancel_topic", "/move_base/cancel")
    )
    cmd_vel_topic = str(
        rospy.get_param("~cmd_vel_topic", "/cmd_vel")
    )

    if duration <= 0.0:
        rospy.logfatal("[nbv_watchdog] duration 必须大于0。")
        return

    if not rospy.get_param("/use_sim_time", False):
        rospy.logfatal(
            "[nbv_watchdog] /use_sim_time 不是 true，"
            "拒绝使用墙钟时间代替仿真时间。"
        )
        return

    cancel_pub = rospy.Publisher(
        cancel_topic,
        GoalID,
        queue_size=1,
    )
    cmd_vel_pub = rospy.Publisher(
        cmd_vel_topic,
        Twist,
        queue_size=1,
    )

    rospy.set_param("/graph_nbv_timeout_reached", False)

    wait_rate = rospy.Rate(10)
    rospy.loginfo("[nbv_watchdog] 等待 Gazebo /clock...")

    while (
        not rospy.is_shutdown()
        and rospy.Time.now().to_sec() <= 0.0
    ):
        wait_rate.sleep()

    if rospy.is_shutdown():
        return

    rospy.loginfo(
        "[nbv_watchdog] /clock 已就绪，等待 Graph NBV 节点..."
    )

    graph_nodes = []

    while not rospy.is_shutdown():
        graph_nodes = find_graph_nbv_nodes(target_node)
        if graph_nodes:
            break
        wait_rate.sleep()

    if rospy.is_shutdown():
        return

    start_time = rospy.Time.now()
    last_reported_interval = -1

    rospy.logwarn(
        "[nbv_watchdog] Graph NBV 已启动：%s；"
        "开始计算 %.1f 秒仿真时间。",
        ", ".join(graph_nodes),
        duration,
    )

    loop_rate = rospy.Rate(10)

    while not rospy.is_shutdown():
        current_nodes = find_graph_nbv_nodes(target_node)

        if not current_nodes:
            rospy.loginfo(
                "[nbv_watchdog] Graph NBV 已自行结束，"
                "看门狗同步退出。"
            )
            return

        elapsed = (rospy.Time.now() - start_time).to_sec()
        report_interval = int(elapsed // 30.0)

        if report_interval != last_reported_interval:
            rospy.loginfo(
                "[nbv_watchdog] 已探索 %.1f 秒，剩余 %.1f 秒。",
                elapsed,
                max(0.0, duration - elapsed),
            )
            last_reported_interval = report_interval

        if elapsed >= duration:
            rospy.logwarn(
                "[nbv_watchdog] 已达到 %.1f 秒仿真时间限制。",
                duration,
            )
            rospy.set_param("/graph_nbv_timeout_reached", True)
            publish_stop(cancel_pub, cmd_vel_pub)

            targets = find_graph_nbv_nodes(target_node)
            if targets:
                try:
                    killed, failed = rosnode.kill_nodes(targets)
                    rospy.logwarn(
                        "[nbv_watchdog] 已关闭节点：%s",
                        killed,
                    )
                    if failed:
                        rospy.logerr(
                            "[nbv_watchdog] 关闭失败：%s",
                            failed,
                        )
                except Exception as exc:
                    rospy.logerr(
                        "[nbv_watchdog] 关闭 Graph NBV 失败：%s",
                        exc,
                    )

            publish_stop(cancel_pub, cmd_vel_pub)
            rospy.logwarn(
                "[nbv_watchdog] 本次 NBV 探索已超时结束。"
            )
            return

        loop_rate.sleep()


if __name__ == "__main__":
    main()

PY

chmod +x \
~/catkin_ws/src/danger_search_robot/scripts/nbv_sim_time_watchdog.py

python3 -m py_compile \
~/catkin_ws/src/danger_search_robot/scripts/nbv_sim_time_watchdog.py
```

创建限时启动文件：

```bash
mkdir -p \
~/catkin_ws/src/danger_search_robot/launch

cat > \
~/catkin_ws/src/danger_search_robot/launch/graph_nbv_stage_b31_manual_gate_timed.launch <<'XML'
<launch>
    <arg name="exploration_dry_run" default="false"/>
    <arg name="exploration_time_limit" default="400.0"/>

    <include file="$(find danger_search_robot)/launch/graph_nbv_stage_b31_manual_gate.launch">
        <arg name="exploration_dry_run"
             value="$(arg exploration_dry_run)"/>
    </include>

    <node pkg="danger_search_robot"
          type="nbv_sim_time_watchdog.py"
          name="nbv_sim_time_watchdog"
          output="screen"
          respawn="false">
        <param name="duration"
               value="$(arg exploration_time_limit)"/>
        <param name="target_node"
               value="/graph_nbv"/>
        <param name="cancel_topic"
               value="/move_base/cancel"/>
        <param name="cmd_vel_topic"
               value="/cmd_vel"/>
    </node>
</launch>
XML
```

这两个文件不需要 `catkin_make`。每层使用同一条启动命令：

```bash
roslaunch danger_search_robot \
graph_nbv_stage_b31_manual_gate_timed.launch \
exploration_dry_run:=false \
exploration_time_limit:=400.0
```

---

**结束条件：** 每层探索已自行完成、人工结束或达到 400 秒仿真时间；机器人返回 `robot_start`，运动停止，三层地图均已保存，危险源结果保存在 `~/catkin_ws/results/detected_danger.json`。
